{
  "name": "cja-passkey-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/passkey-client.ts --bundle --format=iife --global-name=cjaPasskey --target=es2020 --outfile=dist/passkey-client.js",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
